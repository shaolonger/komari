# Komari 安全整改与验收任务清单（最终版）

编制时间：2026-05-24  
适用对象：开发、运维、安全、验收负责人  
配套文档：`Komari安全风险审计与分析报告-final.md`

## 1. 使用说明

本清单用于把审计结论转化为可执行任务。每项任务均包含：

- 目标
- 实施动作
- 验收标准
- 证据留存建议

建议执行方式：

1. 先完成 P0，再安排 P1，最后视场景决定是否执行 P2。
2. 每项任务完成后，由实施人与验收人分别签字或记录到工单系统。
3. 验收必须以“看到配置、命令输出、界面结果或日志证据”为准，不能只口头确认。

## 2. 任务状态约定

| 状态 | 含义 |
| --- | --- |
| 未开始 | 尚未实施 |
| 进行中 | 已开始实施但未完成验收 |
| 已完成 | 已实施且满足验收标准 |
| 已豁免 | 有正式风险接受或场景不适用说明 |

## 3. P0 级任务

### P0-01 关闭 GeoIP 外部查询（状态：已完成）

- 目标：停止节点 IP 与管理员登录 IP 向第三方 GeoIP 服务外发。
- 实施动作：
  - 在系统配置中关闭 GeoIP。
  - 禁止后台自动更新在线 MMDB。
  - 如业务必须保留地理信息，改为离线 MMDB 且仅使用本地文件。
- 验收标准：
  - 后台配置界面或配置文件中 GeoIP 开关为关闭状态。
  - 运行时抓包、代理日志或出口防火墙日志中，不再出现 `ip-api.com`、`ipinfo.io`、`get.geojs.io` 等查询请求。
  - 节点详情页在 GeoIP 关闭后不再依赖在线地理查询仍可正常访问。
- 证据留存：
  - 配置截图。
  - 出口日志或抓包截图。
  - 变更单号。

### P0-02 强制 HTTPS 暴露面板（状态：已完成）

- 目标：防止会话 Cookie、OAuth 状态值和 2FA 初始化敏感数据经明文 HTTP 传输。
- 实施动作：
  - 在前置反向代理上启用 TLS。
  - 配置 HTTP 到 HTTPS 强制跳转。
  - 禁止任何明文 HTTP 登录入口对公网开放。
- 验收标准：
  - 访问 `http://` 地址时自动跳转到 `https://`。
  - 浏览器地址栏显示有效证书，无证书错误。
  - 抓包确认登录请求、Cookie 与 2FA 开启流程均在 TLS 下传输。
- 证据留存：
  - 浏览器跳转截图。
  - TLS 证书信息截图。
  - 抓包或代理验证结果。

### P0-03 立即重置默认管理员密码并清理日志残留（状态：已完成）

- 目标：消除初始化密码或重置密码落入日志后的持续泄露风险。
- 实施动作：
  - 首次启动后立即修改管理员密码为高强度随机密码。
  - 清理包含默认密码或重置密码的容器日志、systemd 日志、运维工单记录和截图。
  - 将新密码纳入合规的密码管理系统。
- 验收标准：
  - 管理员当前密码已变更，不再使用初始化口令。
  - `docker logs`、`journalctl` 或等效日志中不再可检索到初始化口令。
  - 密码已登记到受控的密码管理工具中，而非保存在聊天记录或明文文档中。
- 证据留存：
  - 密码轮换工单。
  - 日志清理前后对比截图。
  - 密码库登记记录。

### P0-04 启用 2FA（状态：已完成）

- 目标：降低单一密码被爆破或泄露后的直接接管风险。
- 实施动作：
  - 为管理员账号启用 TOTP 2FA。
  - 恢复码或种子按组织规范保存。
  - 2FA 开通过程只在 HTTPS 环境下进行。
- 验收标准：
  - 管理员登录流程已要求第二因子。
  - 使用正确密码但缺少 TOTP 时无法登录。
  - 恢复码已安全保存，未出现在聊天工具或普通文档中。
- 证据留存：
  - 配置完成截图。
  - 登录验证录屏或截图。
  - 恢复码托管记录。

### P0-05 取消 root 运行（状态：已完成）

- 目标：降低未来 RCE、恶意二进制执行或供应链问题的后果。
- 实施动作：
  - 裸机部署时将 systemd 服务用户改为专用低权限账号。
  - 容器部署时显式设置非 root `USER`。
  - 确保运行目录权限只授予服务所需最小权限。
- 验收标准：
  - 裸机场景下 `systemctl cat komari` 或服务文件中不再出现 `User=root`。
  - 容器场景下 `docker inspect`、`docker exec id` 或等效命令显示进程非 root。
  - 服务仍可正常启动、写入必要目录并提供核心功能。
- 证据留存：
  - 服务配置截图或容器检查输出。
  - 进程身份输出结果。

### P0-06 管理面网络隔离（状态：已完成）

- 目标：避免 `/api/admin/*` 直接暴露到公网，被撞库、会话劫持或跨站流量直接命中。
- 实施动作：
  - 在反向代理或防火墙上限制管理面仅对内网、VPN 或白名单地址开放。
  - 普通公开节点展示与管理后台分离访问控制。
- 验收标准：
  - 非白名单公网地址无法访问管理接口。
  - 白名单或 VPN 内部地址可正常访问管理面。
  - 外部扫描结果中不再直接暴露管理接口。
- 证据留存：
  - 代理配置片段。
  - 白名单外访问失败截图。
  - 漏扫或端口探测结果。

### P0-07 不在敏感节点本机部署服务端（状态：已完成）

- 目标：避免服务端对外联、日志和控制面风险直接叠加到代理或落地节点本机。
- 实施动作：
  - 将服务端迁移到独立管理平面主机。
  - 受控节点仅保留 Agent 上报链路，不与服务端同机混布。
- 验收标准：
  - 服务端宿主机不再承担代理、中转、落地等敏感业务角色。
  - 资产台账能区分管理平面主机与受控业务节点。
- 证据留存：
  - 资产清单。
  - 拓扑图或部署图。

## 4. P1 级任务

### P1-01 升级 `golang.org/x/image`（状态：已完成）

- 目标：消除 `api/public/mjpeg.go` 字体解析链路上的可达漏洞。
- 实施动作：
  - 升级 `golang.org/x/image` 至 `v0.39.0` 或更高。
  - 重新编译并部署服务端。
- 验收标准：
  - `go.mod` 或依赖锁定结果显示版本不低于 `v0.39.0`。
  - 重新运行 `govulncheck ./...` 时不再报告该漏洞为 reachable。
  - MJPEG 相关功能仍可正常运行。
- 证据留存：
  - 依赖变更 diff。
  - `govulncheck` 输出截图。

### P1-02 升级 `google.golang.org/grpc`（状态：已完成）

- 目标：消除 Nezha 兼容 gRPC 路径上的可达漏洞。
- 实施动作：
  - 升级 `google.golang.org/grpc` 至 `v1.79.3` 或更高。
  - 重新编译并验证兼容服务。
- 验收标准：
  - `go.mod` 或依赖锁定结果显示版本不低于 `v1.79.3`。
  - `govulncheck ./...` 不再报告该漏洞为 reachable。
  - 启用 Nezha 兼容模式时基础连通性正常。
- 证据留存：
  - 依赖变更 diff。
  - 漏洞复测输出。

### P1-03 密码存储改造为现代 KDF

- 目标：提高数据库泄露后的离线爆破成本。
- 实施动作：
  - 将固定盐单轮 SHA-256 改造为 Argon2id、bcrypt 或 scrypt。
  - 为旧密码设计平滑迁移策略。
- 验收标准：
  - 新创建或更新的密码不再使用固定盐单轮 SHA-256。
  - 代码评审确认 KDF 参数达到合理安全基线。
  - 旧账号可平滑迁移或被强制重置，不发生大面积登录中断。
- 证据留存：
  - 代码 diff。
  - 设计说明或迁移方案。
  - 测试记录。

### P1-04 增加登录限速与失败惩罚

- 目标：降低撞库和自动化暴力破解成功概率。
- 实施动作：
  - 对登录接口增加 IP 维度和账号维度限速。
  - 增加失败惩罚、临时锁定或告警机制。
- 验收标准：
  - 连续失败达到阈值后，请求会被限流、延迟或阻断。
  - 日志中可看到失败登录事件与触发限速记录。
  - 正常用户在合理频率下不受明显影响。
- 证据留存：
  - 压测或脚本验证结果。
  - 日志截图。

### P1-05 客户端 token 从 URL 迁移到 Header

- 目标：降低 token 被访问日志、代理和历史记录泄露的概率。
- 实施动作：
  - 将客户端认证 token 改为 HTTP Header 传输。
  - 对旧协议提供过渡兼容窗口，并设定停用时间。
- 验收标准：
  - 新版本客户端不再通过 URL 查询串传输 token。
  - 访问日志中不再出现 `token=` 敏感参数。
  - 旧版本淘汰计划已明确。
- 证据留存：
  - 接口文档变更。
  - 日志样本对比。

### P1-06 禁用 `AllowCors`

- 目标：恢复 WebSocket 同源保护的默认边界。
- 实施动作：
  - 在配置中关闭 `AllowCors`。
  - 如业务确有跨域需要，单独做受控白名单设计，而非全放开。
- 验收标准：
  - 配置项显示 `AllowCors=false`。
  - 使用非同源页面尝试连接 `/api/clients` 与 `/api/rpc2` 时失败。
  - 正常同源前端访问不受影响。
- 证据留存：
  - 配置截图。
  - 跨站连接失败测试结果。

### P1-07 为主题导入增加完整性与目标校验

- 目标：降低远程主题下载链路的供应链与 SSRF 风险。
- 实施动作：
  - 增加允许域白名单。
  - 对重定向目标做再次校验。
  - 引入哈希或签名校验。
- 验收标准：
  - 非白名单域名的主题 URL 被拒绝。
  - 指向私网、环回或经重定向转入私网的 URL 被拒绝。
  - 未提供有效哈希或签名的主题包无法导入。
- 证据留存：
  - 单元测试或手工测试记录。
  - 失败场景截图。

### P1-08 为上传与解压流程增加大小和配额控制

- 目标：降低超大 ZIP 或压缩炸弹导致的 DoS 风险。
- 实施动作：
  - 对上传包体大小、解压总大小、文件数量和单文件大小增加限制。
  - 对解压时间和磁盘占用增加中止阈值。
- 验收标准：
  - 超过阈值的主题或备份上传被拒绝。
  - 压缩炸弹测试样本不会导致服务长时间阻塞或磁盘被打满。
  - 日志中可看到明确的拒绝原因。
- 证据留存：
  - 配置说明。
  - 大包或炸弹样本测试结果。

## 5. P2 级任务

### P2-01 禁用 Cloudflared

- 目标：减少额外出站链路和条件执行链触发面。
- 实施动作：
  - 清空 `KOMARI_CLOUDFLARED_TOKEN` 或等效配置。
  - 关闭后台自动启动 Cloudflared 的能力。
- 验收标准：
  - 服务启动后不再拉起 Cloudflared 进程。
  - 出口连接中不再出现对应 Cloudflare Tunnel 连接。
- 证据留存：
  - 进程列表截图。
  - 网络连接截图。

### P2-02 禁用在线主题、字体与 MMDB 更新

- 目标：把外部内容引入面收缩到最低。
- 实施动作：
  - 停用远程主题导入。
  - 关闭在线字体下载。
  - 关闭在线 MMDB 更新，仅保留离线文件更新。
- 验收标准：
  - 运行时不再访问 GitHub 主题接口、小米字体下载地址和在线 MMDB 更新地址。
  - 本地离线文件方案可正常工作。
- 证据留存：
  - 配置截图。
  - 出口日志截图。

### P2-03 禁用自定义 HTML/JS 注入能力

- 目标：降低 Stored XSS、界面钓鱼和浏览器端恶意脚本执行风险。
- 实施动作：
  - 关闭 Custom Head / Body 等注入能力。
  - 如业务必须保留，仅允许受控模板而非任意脚本。
- 验收标准：
  - 后台不再允许保存任意 HTML/JS 片段，或已切换为白名单模板。
  - 浏览器端页面源代码中不再出现用户自定义任意脚本。
- 证据留存：
  - 界面截图。
  - 页面源代码验证截图。

### P2-04 禁用或隔离 JavaScript 通知发送器与任意 Webhook

- 目标：降低服务端被当作 SSRF 中转点、内网探针或数据回传通道的风险。
- 实施动作：
  - 禁用 JavaScript 通知发送器。
  - 对 Webhook 目标做白名单限制。
  - 对服务端出站流量实施目的地址限制。
- 验收标准：
  - 未授权 URL 无法被配置为通知目标。
  - 服务端无法访问未授权内网地址、metadata 地址或任意公网测试地址。
  - 合法通知渠道仍能正常工作。
- 证据留存：
  - 防火墙策略。
  - 拒绝访问测试结果。

### P2-05 收紧宿主机出站访问控制

- 目标：即使面板失陷，也尽量限制其对外或对内探测能力。
- 实施动作：
  - 采用主机防火墙或云安全组限制出站地址。
  - 仅放行 Agent 上报、必要通知和受控依赖地址。
- 验收标准：
  - 默认拒绝未授权出站访问。
  - 受控节点上报链路、管理面访问和必要通知不受影响。
  - 使用测试 URL、metadata 地址、任意公网探测地址时均被阻断。
- 证据留存：
  - 防火墙规则截图。
  - 连通性测试记录。

## 6. 建议验收顺序

建议按以下顺序组织实施与验收：

1. 先完成全部 P0 项，再允许服务继续进入敏感环境。
2. 再完成 P1 项，把当前已确认的高概率利用面和可达依赖漏洞收掉。
3. 若场景涉及梯子、代理、落地机、中转 VPS 或高对抗部署，再执行全部 P2 项。

## 7. 交付物清单

建议每项整改至少留存以下材料：

- 变更单或工单编号
- 配置变更截图或代码 diff
- 验收测试结果
- 回滚方案
- 实施人与验收人记录

## 8. 最终验收门槛

若要认定“Komari 已达到可进入高敏感场景的基础加固状态”，最低门槛应为：

1. 全部 P0 项完成并验收通过。
2. P1-01、P1-02、P1-03、P1-04、P1-05 至少完成并验收通过。
3. 若部署在代理、落地机、中转 VPS 或类似高敏感场景，P2 项不得长期豁免，除非有正式风险接受文件。

在未达到上述门槛前，不应把该系统视为适合直接用于高敏感基础设施的默认可上线方案。