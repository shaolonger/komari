# Fleet Telegram Operations Report Todo

本清单用于实现 Komari Telegram JavaScript 通知的全局运维报告能力。日报定义修正为“前一日完整运维简报”：触发时间可以是用户指定时区的某个小时，但统计窗口必须是该时区的前一自然日，而不是当天刚开始的数据。

## Product Decisions

- 日报：统计用户指定时区的前一自然日。
- 周报：统计用户指定时区的上一 ISO 周。
- 月报：统计用户指定时区的上一自然月。
- 时区设置：报告触发时刻、统计窗口、报告正文里的时间都以该设置为准，不依赖服务器部署时区。
- Telegram 展示：JS 脚本负责排版和发送，后端负责计算。能用视觉图表表达的地方优先使用 Telegram 友好的文本图表、进度条和排行条，避免脚本侧拉取大量数据或依赖外部图片服务。

## Todo

- [x] T0 保存本地执行清单，明确日报定义、时区原则、Telegram 视觉化边界和验收标准。
- [ ] T1 在 Komari 后端增加全局报告配置模型与管理接口，支持启用/关闭日报、周报、月报、Top N 和最后发送时间。
- [ ] T2 增加通知报告时区设置，确保触发判断、统计窗口和报告时间显示都按该时区执行。
- [ ] T3 增加全 VPS 运维报告生成器：概览、异常、建议动作、CPU/内存/磁盘/负载/流量/Ping/覆盖率榜单。
- [ ] T4 将全局报告调度接入服务定时任务，保证同一周期不重复发送，通知关闭时不消耗周期。
- [ ] T5 优化 Telegram JavaScript 示例，支持全局运维报告的视觉化排版、排行条、健康分和异常摘要。
- [ ] T6 增加并运行测试：时区窗口、周期去重、报告统计、接口校验、JS 语法与渲染烟测。
- [ ] T7 更新版本号，打 tag，push 到远程 GitHub，并创建新的 GitHub Release。

## Acceptance

- 用户可配置报告时区，例如 `Asia/Shanghai`、`America/Los_Angeles`。
- 日报在配置时区的发送小时触发，内容统计“前一日 00:00:00 到今日 00:00:00”。
- 周报/月报只在配置时区的周一/每月 1 日触发，并统计已完整结束的周期。
- Telegram 报告能一眼看到全局健康、异常数量、最需要关注的节点和各项 Top 榜单。
- 后端生成结构化 `FleetReport` 事件，JS 脚本只负责视觉化展示和投递。
- 所有新增后端测试、全量 Go 测试、JS 语法检查和渲染烟测通过。

