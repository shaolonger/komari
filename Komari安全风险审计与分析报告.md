# Komari 安全风险审计与分析报告

审计时间：2026-05-23  
审计对象：Komari 当前工作区源码  
审计方式：静态代码审计 + 依赖与调用链扫描（`govulncheck`）+ 部署脚本与外联链路核查

## 1. 审计范围与结论摘要

### 1.1 审计范围

本次审计覆盖了以下内容：

- 服务端入口、路由与鉴权逻辑
- WebSocket、JSON-RPC、终端桥接、任务下发能力
- 主题导入、备份恢复、GeoIP、Cloudflared、字体下载等外联与文件处理逻辑
- 登录、会话、2FA、OAuth、客户端 token 使用方式
- Dockerfile、安装脚本、Go 依赖与已知漏洞

未覆盖或无法完全确认的部分：

- 真正执行远程命令的“Agent 本体”不在当前仓库实体文件中，因此无法对 Agent 端命令执行细节做完整代码级确认
- 未进行动态渗透测试、浏览器联动测试、容器逃逸测试和生产环境抓包
- 未对第三方 GitHub Release、Cloudflared 二进制、主题 ZIP 等外部内容做来源真实性核验

### 1.2 项目作用

从源码来看，Komari 不是一个“只读监控看板”，而是一个带有管理能力的服务器监控控制面，主要功能包括：

- 接收客户端/探针上报的机器状态、Ping、流量和记录数据
- 通过 WebSocket 和 JSON-RPC 向前端提供实时节点信息
- 允许管理员对节点发起交互式终端会话
- 允许管理员向节点下发“执行命令”类任务
- 提供主题导入、配置注入、通知脚本、自定义外联、Cloudflared 隧道等扩展能力

这意味着它一旦被正确部署，可以很好用；但一旦管理员账号失陷，它不是“泄露一份监控数据”那么简单，而是可能成为整个受控节点群的跳板。

### 1.3 总体结论

结论分三层：

1. **未发现源码内置后门、矿工、隐藏下载执行链或明显恶意逻辑。** 从仓库代码本身看，它不像带毒项目。
2. **该项目存在多项真实安全风险，且不少风险与默认部署方式直接相关。** 其中最值得重视的是：IP 外泄、弱会话/口令保护、外部文件与主题供应链风险、管理面失陷后的节点级控制风险。
3. **如果你打算把它用于“梯子/VPN/代理/VPS 节点”这类高敏感基础设施，默认配置并不适合直接上线。** 至少要先做一轮安全加固。

## 2. 风险总表

| 编号 | 风险项 | 级别 | 是否默认存在 | 核心结论 |
| --- | --- | --- | --- | --- |
| R1 | GeoIP 导致被监控节点 IP 外发 | 高 | 是 | 节点 IP 会被发往第三方 GeoIP 服务，敏感场景不应接受 |
| R2 | 会话 cookie 未启用 Secure | 高 | 是 | 明文 HTTP 部署时，登录态与 2FA 初始化密钥可被窃取 |
| R3 | 密码哈希过弱且缺少登录限速 | 高 | 是 | 使用固定盐单轮 SHA-256，不适合作为现代密码存储方案 |
| R4 | 客户端 token 经常出现在 URL 查询串 | 高 | 是 | 容易被反向代理、访问日志、历史记录和监控系统泄露 |
| R5 | 主题导入/更新供应链风险 | 高 | 否 | 可从任意 URL/GitHub Release 拉主题 ZIP，无签名校验 |
| R6 | 管理面失陷后可横向控制节点 | 高 | 是 | 服务端支持终端桥接和命令任务分发，失陷后危害很大 |
| R7 | 安装脚本/二进制/Cloudflared 下载无校验 | 高 | 是 | `latest` 直下直用，缺少 checksum/signature 验证 |
| R8 | 自定义 HTML/主题/脚本可注入前端恶意代码 | 中高 | 否 | 会影响访问仪表盘的管理员或访客浏览器 |
| R9 | 服务器侧 SSRF/外联能力较强 | 中高 | 否 | JS 通知脚本、Webhook、主题下载等可向任意 URL 发请求 |
| R10 | `AllowCors` 打开后 WebSocket 同源保护失效 | 中 | 否 | 会放宽 `/api/clients` 与 `/api/rpc2` 的跨站访问边界 |
| R11 | 已确认 2 个可达依赖漏洞 | 中 | 视功能而定 | 一个在 MJPEG 字体解析，一个在 gRPC 兼容服务 |
| R12 | 默认以 root 运行或容器内 root 运行 | 中 | 常见 | 放大一切未来 RCE 或恶意二进制执行的后果 |
| R13 | QQ OAuth 跳过严格 state 校验 | 中 | 否 | 仅在启用 QQ 登录时 relevant |
| R14 | 大文件/压缩包缺少体积限制 | 中 | 否 | 管理员接口可被用于磁盘或内存 DoS |
| R15 | 公开接口默认不直接暴露完整节点 IP | 低风险正向项 | 是 | 默认公开 REST 已清空 IP，JSON-RPC 默认也不返回完整 IP |

## 3. 与“IP 泄露”直接相关的结论

这是你最关心的部分，单独展开。

### 3.1 会泄露谁的 IP

从代码看，可能被外发或暴露的 IP 有三类：

1. **被监控节点的公网 IP**
2. **部署 Komari 的服务端主机公网 IP**
3. **管理员登录时的来源 IP**

### 3.2 被监控节点 IP 外泄：结论为“是，且默认存在”

风险路径：

- `api/client/uploadBasicInfo.go` 在客户端未主动提供 IP 时，会使用 `c.ClientIP()` 作为节点 IP
- `utils/geoip/geoip.go` 在 GeoIP 启用时会调用外部 GeoIP provider
- 默认 provider 逻辑在 `utils/geoip/geoip.go` 中指向 `ipinfo`
- 可选 provider 包括：
  - `utils/geoip/ipinfo.go` -> `https://ipinfo.io/{ip}/json`
  - `utils/geoip/ipapi.go` -> `http://ip-api.com/json/{ip}...`
  - `utils/geoip/geojs.go` -> `https://get.geojs.io/...`

结论：

- **只要启用 GeoIP，就会把节点 IP 发给第三方服务。**
- `ip-api` 还是 **明文 HTTP**，这意味着不只是第三方服务商知道，被路径上的网络观察者也能直接看到你查询了哪个节点 IP，还可能篡改返回结果。
- 如果你的节点本身是高敏感资产，例如代理节点、落地机、中转机，这种外发行为本身就应视为不可接受。

### 3.3 服务端主机 IP 外泄：结论为“有多条外联链路，会留下出站痕迹”

服务端会主动访问外部站点的路径包括：

- `utils/geoip/mmdb.go` 下载 MMDB：`raw.githubusercontent.com`
- `api/admin/theme.go` 访问 `api.github.com` 或任意远程主题 URL
- `api/public/mjpeg.go` 下载字体：`https://hyperos.mi.com/font-download/MiSans.zip`
- `utils/cloudflared/cloudflared.go` 启动 Cloudflared 后与 Cloudflare 建立隧道
- `install-komari.sh` 和 `Dockerfile` 从 GitHub / Cloudflare 下载二进制
- 各类通知渠道、Webhook、OAuth provider 会访问对应外部域名

结论：

- 即使不考虑节点 IP，**服务端主机的公网 IP 也会因为这些外联被多个第三方看到**。
- 如果你把 Komari 面板放在一个不希望暴露出站行为特征的机器上，这也是需要规避的。

### 3.4 管理员或访问者 IP 外泄：结论为“有条件发生”

`database/accounts/sessions.go` 中创建登录会话时，如果启用了登录通知，会对登录 IP 再做一次 GeoIP 查询。

这意味着：

- **管理员登录 IP** 也可能被发送给 GeoIP provider
- 如果你自己通过敏感网络环境访问面板，这个来源 IP 同样可能外发

### 3.5 公开页面是否默认向访客暴露节点 IP

这里需要区分：

- `api/public/nodes.go`：默认会把 `IPv4` 和 `IPv6` 清空，不直接向未登录访客返回节点 IP
- `api/jsonRpc/common.go`：`send_ip_addr_to_guest` 打开时，访客可获得 **打码后的 IP 前缀**，不是完整 IP；关闭时则返回空字符串

结论：

- **默认公开 REST 接口不会把完整节点 IP 暴露给访客。**
- 但如果你开启 `send_ip_addr_to_guest`，访客仍会拿到部分地址前缀信息，这在某些场景下仍然有识别价值。

### 3.6 针对“梯子/代理节点”场景的判断

如果你是用 Komari 去监控代理节点、落地机、机场后端 VPS 或其它高敏感 IP：

- 默认 GeoIP 行为 **不安全**
- `ip-api` 的 HTTP 查询 **尤其不安全**
- Cloudflared、字体下载、MMDB 下载、主题下载会给外界留下 **额外出站指纹**

对这类场景，建议：

- 直接关闭 GeoIP
- 不使用在线更新 MMDB、字体、主题
- 不启用 Cloudflared
- 不把面板部署在敏感节点本机上，而是单独放在管理平面机器

## 4. 外部文件、远程资源与供应链风险

### 4.1 默认前端资源本身不是 CDN 动态注入模式

这是一个正向结论：

- 默认前端资源是本地内置/本地主题资源，不是运行时从公共 CDN 拉一堆 JS
- 因此“访问页面时被第三方 CDN 临时注入恶意脚本”的典型风险，不是这个项目的主要问题

但这不代表“外部文件注入”风险不存在，因为本项目本身提供了多个把外部内容引入运行面的入口。

### 4.2 主题导入与更新存在明显供应链风险

关键代码在 `api/admin/theme.go`：

- 支持从任意 URL 下载 ZIP
- 支持把 GitHub 仓库解析为 latest release 资产再下载
- 下载后仅做 ZIP 与主题格式校验
- **没有做签名校验、哈希校验、发布者校验、允许域白名单校验**

风险含义：

- 如果管理员导入了恶意主题，主题中的 HTML/JS/CSS 就会在仪表盘中运行
- 这相当于把“第三方前端代码”正式引入你的监控面板
- 一旦主题作者、主题仓库、GitHub 账号、Release 资产或下载 URL 被污染，就可能把恶意脚本带入管理面

这类恶意脚本可以做什么：

- 窃取管理员会话
- 偷发 API 请求
- 修改展示结果
- 给访问者浏览器下发恶意内容
- 在页面层面做钓鱼、伪造登录界面、插入挖矿脚本

### 4.3 SSRF 防护存在，但不完整

`api/admin/theme.go` 的 `downloadThemeFromURL` 做了基础 SSRF 防护：

- 只允许 `http/https`
- 解析主机名并拦截私网/环回地址

这是正向项，但仍然 **不完整**，原因包括：

- `http.Get` 默认会跟随重定向，代码没有对重定向后的目标再次做私网检查
- 仅在请求前解析一次 DNS，无法覆盖重定向和部分 DNS 变化场景

因此更准确的结论是：

- **不是“完全无防护”**
- **但也远称不上“足以抵御 SSRF”**

### 4.4 安装脚本、二进制与 Cloudflared 下载缺少完整性校验

高风险点：

- `README.md` 与 `docs/README_zh.md` 直接建议 `curl` 下载并执行安装脚本
- `install-komari.sh` 从 GitHub Release `latest` 下载 Komari 二进制，不校验 checksum/signature
- `Dockerfile` 从 Cloudflare GitHub Release 下载 `cloudflared`，同样不校验 checksum/signature

结论：

- 这是典型的供应链薄弱点
- 一旦下载源、Release 资产、链路、镜像构建机或上游账号出现问题，恶意文件会被直接部署

### 4.5 字体下载链路也属于外部文件注入面

`api/public/mjpeg.go` 会从固定地址下载 `MiSans.zip`，然后在服务端解析字体。

风险不在于“这个域名一定有问题”，而在于：

- 它是外部 ZIP 文件
- 会被服务端主动下载并解析
- 当前依赖扫描已经证实这一链路上存在可达漏洞，见后文 R11

## 5. 是否存在“挖矿”“木马”“恶意执行”风险

### 5.1 是否发现项目自带矿工或恶意挖矿逻辑

结论：**未发现。**

审计依据：

- 对源码中矿池、矿工、常见矿工关键字检索，未在业务代码中发现对应实现
- 未发现连接矿池、下载矿工、周期性算力任务等逻辑
- 全仓库显式 `os/exec` 的核心业务使用点主要是 `utils/cloudflared/cloudflared.go`

因此，从当前仓库看：

- **它不是一个自带挖矿的项目**
- **没有看到“安装后偷偷在服务器上跑矿工”的直接证据**

### 5.2 但它是高价值控制面，管理面失陷后可被用来挖矿

这里要严格区分“项目自身带毒”和“项目被拿来作恶”两件事。

从源码看，Komari 服务端具备两类非常强的能力：

- `api/admin/exec.go`：向在线客户端下发命令任务
- `api/terminal/*`：为管理员和客户端建立终端桥接

这意味着：

- 如果管理员账号被拿下，攻击者就可能利用 Komari 去控制受管节点
- 这些节点随后可以被用来下载矿工、跑扫描、做代理、打内网、持久化后门

所以关于“会不会被拿去挖矿”的准确回答是：

- **项目本身没有内置挖矿代码**
- **但它天然具备被攻击者当成批量控机面板使用的价值**

### 5.3 对“部署这台机器本身被用来挖矿”的判断

从当前仓库代码本身看，管理员并没有现成的“在 Komari 服务端宿主机上执行任意 shell 命令”的正规功能。

也就是说：

- 失陷后更直接的受害对象是 **受控节点**
- 而不是面板宿主机立刻被项目内置功能直接挖矿

但仍有两个需要重视的放大因素：

1. `install-komari.sh` 生成的 systemd 服务明确使用 `User=root`
2. `Dockerfile` 未设置 `USER`，容器默认以 root 运行

这表示：

- 一旦未来出现真正的服务端 RCE、恶意二进制执行、依赖漏洞利用或供应链中招，后果会被明显放大

### 5.4 一个值得注意的条件执行链：恶意 Cloudflared 二进制

`utils/cloudflared/cloudflared.go` 的 `resolveBinaryPath()` 会尝试从以下位置寻找可执行文件：

- `cloudflared`
- `data/cloudflared`
- `data/bin/cloudflared`
- 系统路径等

这本身不一定是漏洞，但要注意：

- 备份恢复功能可以把文件写入 `./data`
- 非 Docker 且系统未安装 Cloudflared 的场景下，`data/cloudflared` 可能成为候选可执行文件

因此如果管理员面已经失陷，攻击者可通过“写入恶意二进制 + 触发 Cloudflared 启动”尝试获得宿主机代码执行。这个链路不是默认自动触发，但确实值得防范。

## 6. 鉴权、口令、会话与管理面风险

### 6.1 密码存储方式偏弱

`database/accounts/accounts.go` 使用的是：

- 固定常量盐 `constantSalt`
- 单轮 SHA-256

这不符合现代密码存储最佳实践。现代推荐通常至少应使用：

- Argon2id
- scrypt
- bcrypt

当前实现的问题：

- 固定盐不可抵御同口令复用带来的模式泄露
- 计算成本低，数据库泄露后更利于离线爆破

### 6.2 没有看到登录速率限制、失败锁定或验证码

本次审计没有在登录路径上看到：

- rate limiting
- 登录失败惩罚
- IP/账号维度锁定
- 验证码

这会放大弱口令与撞库风险。

### 6.3 会话 cookie 未设置 Secure

以下 cookie 都是用 `SetCookie(..., false, true)` 设置的，也就是：

- `Secure = false`
- `HttpOnly = true`

受影响的至少包括：

- `session_token`
- `oauth_state`
- `binding_external_account`
- `2fa_secret`

影响：

- 如果你通过明文 HTTP 暴露面板，这些 cookie 会跟着 HTTP 传输
- 局域网嗅探、网关、代理、恶意 Wi-Fi、旁路设备都可能窃取它们
- `2fa_secret` 被放在 cookie 中，意味着 2FA 开通过程 **必须** 在 HTTPS 下进行，否则连 2FA 秘钥本身都可能泄露

### 6.4 默认管理员口令会写入日志

`cmd/server.go` 在首次初始化管理员时会打印：

- 用户名
- 密码

此外 `cmd/chpasswd.go` 也会直接打印新密码。

这会带来以下现实风险：

- Docker 日志可被旁观者读取
- `journalctl`、集中日志、宿主机日志代理可能保留明文密码
- 运维截图、工单、监控系统也可能把这串密码扩散出去

### 6.5 QQ OAuth 的 state 校验被放宽

`api/public/oauth.go` 对 QQ provider 特别处理为：

- 只要求 state 非空
- 不强制要求 `cookie state == query state`

这比标准 OAuth/OIDC 的 state 校验弱，属于条件性风险：

- 只在启用 QQ 登录时 relevant
- 但确实降低了 CSRF 防护强度

### 6.6 客户端 token 放在 URL 查询串，属于高风险设计

多个客户端接口直接从查询参数读取 token，例如：

- `api/Auth.go`
- `api/client/report.go`
- `api/client/uploadBasicInfo.go`
- `api/client/task.go`

这类设计的典型问题是：

- 被 Nginx/Caddy/Ingress/边车日志记录
- 被浏览器历史、代理、APM、链路追踪系统记录
- 被 Referer、错误页、调试工具扩散

一旦客户端 token 泄露，攻击者可能伪造节点上报、接管节点身份或干扰状态数据。

## 7. 跨域、WebSocket、CSRF 与边界问题

### 7.1 默认同源检查整体不差，但 `AllowCors` 会明显放宽边界

正向项：

- `ws/origin.go` 默认要求 `Origin.Host == Request.Host`
- 终端请求端默认使用同源校验

风险点：

- `api/ws.go` 中 `/api/clients` 的 WebSocket，在 `AllowCors=true` 时会放行任意 Origin
- `api/jsonRpc/JsonRpc.go` 中 `/api/rpc2` 的 WebSocket，在 `AllowCors=true` 时也会放行任意 Origin

这意味着：

- 如果管理员在浏览器中已登录 Komari
- 且你把 `AllowCors` 打开
- 恶意站点就更容易借助浏览器去对 Komari 建立跨站 WebSocket 连接

从安全角度，`AllowCors` 不是“前端兼容小选项”，而是一个会显著改变边界的配置。

### 7.2 REST 层没有看到通用 CSRF 防护

没有看到统一的 CSRF token 机制。

虽然现代浏览器默认对未显式声明的 cookie 往往采用 `SameSite=Lax`，这会缓解一部分跨站 POST，但这不是完整的 CSRF 防线，特别是在：

- 某些 GET 状态修改接口存在时
- WebSocket 不受传统 CORS 规则约束时
- 反向代理、自定义前端、老旧浏览器或特定嵌入场景下

不能把“浏览器默认行为”当成正式安全设计。

### 7.3 代理头与 Host 头使用较为宽松

`utils/gin.go` 的 OAuth 回调 URL 生成依赖：

- `X-Forwarded-Proto`
- `X-Forwarded-Protocol`
- `X-Forwarded-Ssl`
- `X-Url-Scheme`
- `Request.Host`

而代码中没有看到显式的 Gin trusted proxies 配置。

这不是立刻可用的高危漏洞结论，但它意味着：

- 你的反向代理配置必须正确
- 不能随便信任外部可控代理头
- OAuth 回调和绝对 URL 生成逻辑需要依赖干净的反向代理边界

## 8. 文件上传、恢复、主题与路径穿越分析

### 8.1 正向结论：主题 ZIP 与备份 ZIP 都做了路径穿越保护

这部分我专门核查了，因为它常见又危险。

正向项：

- `api/admin/theme.go` 解压主题前，对目标路径做了前缀约束
- `database/dbcore/dbcore.go` 的 `unzipToDir()` 对备份恢复也做了 `filepath.Clean` + 目标目录前缀校验

结论：

- **没有看到明显的 Zip Slip / 路径穿越写任意路径漏洞**

### 8.2 但没有看到显式文件大小/解压体积限制

这意味着存在条件性 DoS 风险：

- 主题上传可读入整个请求体
- 备份上传与恢复会占用磁盘与 IO
- 没有看到对压缩包解压后体积的强约束

如果管理员账号被盗，攻击者可用超大 ZIP、压缩炸弹、磁盘填满等方式造成服务不可用。

## 9. 服务器端主动外联与 SSRF 面分析

### 9.1 通知脚本 JavaScript 具备任意 HTTP 请求能力

`utils/messageSender/javascript/apis.go` 中，注入给 JS 的 `fetch` 和 `XMLHttpRequest`：

- 允许自定义 URL
- 允许自定义 method/header/body
- 没有看到私网地址、metadata 地址、localhost 等限制

因此结论非常明确：

- **一旦管理员权限被拿到，攻击者可以把 Komari 服务器当作一个 HTTP 代理/探针使用**
- 可用于探测内网、访问云平台元数据、访问仅服务器本机可见的地址、向外回传数据

### 9.2 这不是“任意系统命令执行”，但足以形成 SSRF/数据探测风险

需要准确描述：

- 这套 JS API 目前主要是网络请求能力，不是宿主机 shell 能力
- 所以它更像 SSRF / 出站代理 / 数据回传，而不是直接 OS RCE

但在实战里，这依然足够危险。

### 9.3 其他可配置外联面

还存在多个管理员可配置或可触发的主动外联：

- Webhook 通知
- Telegram/Bark/ServerChan 等通知渠道
- OAuth provider
- 主题更新
- GeoIP 更新与查询

因此如果你的 threat model 是“面板哪怕失陷，也不希望它主动碰外网/内网”，那 Komari 本身并不属于这类极简隔离产品。

## 10. 依赖漏洞扫描结果

本次运行 `govulncheck ./...`，扫描结果显示当前代码 **可达** 的漏洞有 2 个：

### 10.1 GO-2026-4962: `golang.org/x/image`

- 模块版本：`golang.org/x/image@v0.35.0`
- 修复版本：`v0.39.0`
- 风险：处理恶意 SFNT 字体时可能触发过量内存分配
- 可达调用链：`api/public/mjpeg.go` 中字体下载与字体解析路径

影响解释：

- 如果攻击者能影响或替换被下载/解析的字体文件，可能造成内存消耗型 DoS
- 结合 `api/public/mjpeg.go` 的自动字体下载逻辑，这个风险不是纯理论上的“库里有漏洞”，而是代码路径可达

### 10.2 GO-2026-4762: `google.golang.org/grpc`

- 模块版本：`google.golang.org/grpc@v1.75.0`
- 修复版本：`v1.79.3`
- 风险：特定 `:path` 情况下的授权绕过问题
- 可达调用链：`cmd/nezha_compat.go` 中的 Nezha 兼容 gRPC 服务

影响解释：

- 只在你启用了 Nezha 兼容 gRPC 服务时 relevant
- 但既然调用链可达，就不应忽略

### 10.3 其他扫描结论

扫描还提示：

- 还有若干“已导入但当前代码似乎未走到”的漏洞
- 以及若干“被依赖但当前调用链未触达”的漏洞

因此不能把“现在只有 2 个 reachable”理解为“整个依赖绝对没问题”，而应理解为：

- 当前最需要优先修的至少是这 2 个

## 11. 已确认的正向安全项

为了避免报告只讲风险，这里也列出几个本次确认到的正向点：

1. **未发现内置矿工、后门、隐藏木马逻辑。**
2. **默认访客接口不会直接暴露完整节点 IP。**
3. **主题解压与备份恢复都做了路径穿越保护。**
4. **自动发现注册不是匿名开放的，需要 `AutoDiscoveryKey`。**
5. **Cloudflared 运行参数里带了 `--no-autoupdate`，运行时自动自更新风险较低。**
6. **默认 WebSocket 同源检查并非完全放飞，而是和 Host 绑定；问题主要出在管理员主动开启 `AllowCors` 的场景。**

## 12. 重点回答用户关心的问题

### 12.1 “部署后是否会导致 IP 泄露？”

**会，且默认就有。**

最重要的泄露路径是：

- 节点 IP -> 第三方 GeoIP 服务

其他会留下外联痕迹的路径还有：

- 服务端 -> GitHub / Cloudflare / Xiaomi 字体站 / 主题站点 / 通知目标 / OAuth provider

如果你的环境对匿名性要求很高，应把这些链路全部关掉或离线化。

### 12.2 “引用的外部文件是否存在被注入恶意代码风险？”

**有，而且主要体现在主题、安装链路和外部二进制，不在默认嵌入前端本身。**

高风险点：

- 远程主题 ZIP
- 安装脚本与 Release 二进制
- Cloudflared 二进制
- 字体 ZIP
- MMDB 下载文件

这些都缺少完整性校验或签名验证。

### 12.3 “会不会利用部署机器挖矿？”

**未发现项目自带挖矿。**

但要区分两件事：

- 项目自己偷偷挖矿：未发现
- 攻击者拿下面板后利用它去控制节点、部署矿工：完全有可能，而且风险很现实

### 12.4 “会不会直接把节点 IP 暴露给普通访客？”

**默认不会完整暴露。**

但：

- GeoIP 会把节点 IP 发给第三方
- JSON-RPC 在开启 `send_ip_addr_to_guest` 后会向访客返回打码后的 IP 前缀

## 13. 安全部署建议清单

按优先级排序，建议至少做到以下事项：

### P0：上线前必须做

1. 关闭 GeoIP，或改成本地离线 MMDB 且禁止在线更新。
2. 只允许 HTTPS 访问面板，绝不要裸露 HTTP 登录。
3. 首次启动后立刻修改管理员密码，并清理包含明文密码的日志。
4. 开启 2FA。
5. 不要用安装脚本默认的 root 方式运行，改为专用低权限用户。
6. 不要把面板部署在敏感节点本机上。
7. 禁止公网直接暴露 `/api/admin/*`，至少放到反向代理后并加 IP 白名单。

### P1：强烈建议做

1. 升级依赖：
   - `golang.org/x/image` >= `v0.39.0`
   - `google.golang.org/grpc` >= `v1.79.3`
2. 把密码存储改为 Argon2id/bcrypt/scrypt。
3. 给登录接口加速率限制、失败惩罚和审计告警。
4. 客户端 token 改走 Header，不再放在 URL 查询串。
5. 禁用 `AllowCors`，除非你明确知道为什么需要它。
6. 对主题导入加域名白名单、重定向复检、hash/signature 校验。
7. 给上传 ZIP 和解压流程增加大小限制与解压配额。

### P2：敏感场景建议做

1. 禁用 Cloudflared。
2. 禁用在线主题导入、在线字体下载、在线 MMDB 更新。
3. 禁用自定义 HTML/JS 注入功能。
4. 禁用或隔离 JavaScript 通知发送器与任意 Webhook。
5. 将面板所在主机出站策略收紧，只允许必要目的地址。

## 14. 最终判断

如果把 Komari 当成“普通的自建监控看板”，它在功能上是完整的，代码里也**没有看到内置恶意行为**。

但如果把它放到安全要求较高的环境里看，它当前的真实问题也很明确：

- 默认会把节点 IP 外发给第三方 GeoIP 服务
- 默认会话保护偏弱，不适合明文 HTTP 暴露
- 外部文件和主题供应链风险不小
- 管理面一旦失守，后果不是只读泄露，而是可能扩散到全部受控节点

因此最终结论是：

- **可用，但不能按默认思路直接用于高敏感基础设施**
- **不建议未经加固就部署到与“梯子/代理/落地机/高敏感 VPS”直接相关的环境**
- **如果必须用，至少先完成 P0 和 P1 级别的加固**
